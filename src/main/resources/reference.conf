include "messages/messages.conf"

db {
  driver = "org.postgresql.Driver"
  url = ${?DATABASE_URL}
}

bot {
  port = 8080
  port = ${8088}
  token = ${}
  url = ${https://t.me/Withme1bot}
  username = "Withme1bot"
  username = ${Withme1bot}
}
